using Xunit;

namespace Granit.OpenIddict.Server.Tests;

/// <summary>
/// Placeholder to prevent "no test available" warnings.
/// Replace with real tests when OpenIddict server logic requires unit testing.
/// </summary>
public sealed class PlaceholderTests
{
    [Fact]
    public void Project_is_discoverable() =>
        Assert.True(true);
}
