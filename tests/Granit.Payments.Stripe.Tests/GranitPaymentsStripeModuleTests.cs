using Xunit;

namespace Granit.Payments.Stripe.Tests;

public sealed class GranitPaymentsStripeModuleTests
{
    [Fact]
    public void Module_can_be_instantiated()
    {
        // Placeholder — replace with real tests
        Assert.True(true);
    }
}
