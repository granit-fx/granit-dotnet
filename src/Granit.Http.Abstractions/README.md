# Granit.Http.Abstractions

Lightweight HTTP abstractions for Granit endpoints: marker interfaces and metadata attributes (e.g. idempotency). Zero dependencies beyond the ASP.NET Core shared framework.

Part of the [granit](https://granit-fx.dev) framework.
