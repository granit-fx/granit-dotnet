{
  "_comment": "Stub config for build-time OpenAPI generation only. The generator strips all options validators, so values need not be real — they exist solely so config-collection-driven modules emit their full contract. BFF maps one route group per configured frontend; without an entry it emits zero endpoints.",
  "Bff": {
    "Frontends": [
      {
        "Name": "app"
      }
    ]
  }
}
