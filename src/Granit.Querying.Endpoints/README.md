# Granit.Querying.Endpoints

Minimal API endpoints for Granit.Querying. Provides `MapQueryEndpoints<T, TDto>()`
fluent API, `filter[field.op]=value` query string binding, `GET /meta` metadata
endpoint, CRUD SavedViews endpoints, and OpenAPI documentation conventions.

Part of the [granit](https://granit-fx.dev) framework.

## Installation

```bash
dotnet add package Granit.Querying.Endpoints
```

## Dependencies

- `Granit.Http.ApiDocumentation`
- `Granit.Authorization`
- `Granit.Guids`
- `Granit.Querying`
- `Granit.Timing`
- `Granit.Validation`

## Documentation

See the [full documentation](https://granit-fx.dev).
